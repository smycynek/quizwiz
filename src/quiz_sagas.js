import { call } from 'redux-saga/effects';
import { Types } from './types';

export function* createQuizApiSaga(api, name) {
  // const data = yield call(api.createQuiz, name);
  // yield put({type: Types.CREATE_QUIZ, name, data.id})  //verify this his righ
  // add failure case
}
